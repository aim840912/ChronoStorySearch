'use client'

/**
 * 篩選按鈕元件
 * 目前所有按鈕已移至其他元件：
 * - 進階篩選按鈕 → SearchBar
 * - 清除篩選按鈕 → AdvancedFilterPanel
 * - 市場刊登按鈕 → 暫時移除
 *
 * 保留此元件以便未來重新啟用市場功能
 */
export function FilterButtons() {
  return null
}
