# 自訂 Slash 指令使用指南

本指南說明如何使用和擴展專案的自訂 slash 指令。

## 📑 目錄

- [專案內建指令](#專案內建指令)
- [使用方法](#使用方法)
- [建立自訂指令](#建立自訂指令)
- [進階功能](#進階功能)
- [最佳實踐](#最佳實踐)
- [疑難排解](#疑難排解)

## 🎯 專案內建指令

本專案在 `.claude/commands/` 目錄中提供 4 個精心設計的自訂指令：

### 1. `/ultraplan` - Ultra Planning Mode

**用途**：使用 ultrathink 進行深度規劃（Plan Mode）

**Token 預算**：31,999（最大）

**適用場景**：
- ✅ 複雜的系統架構設計
- ✅ 需要評估多種方案的決策
- ✅ 大型功能的實作規劃
- ✅ 技術選型評估

**基本用法**：
```bash
# 切換到 Plan Mode
Shift+Tab

# 使用指令
> /ultraplan 實作即時聊天功能，支援私訊和群組聊天

# 或者規劃架構
> /ultraplan 設計一個可擴展的通知系統
```

**指令會要求 Claude**：
1. 深度分析並評估至少 3 種方案
2. 列出每種方案的優缺點（可維護性、效能、成本、技術債）
3. 提供推薦方案的詳細計劃（架構、步驟、風險、測試）
4. 考慮專案脈絡（Next.js 15、Supabase、Redis、CLAUDE.md 規範）

**輸出範例**：
```markdown
# 即時聊天功能實作規劃

## 方案評估

### 方案 A: WebSocket + Redis Pub/Sub
- 優點：真正的雙向通訊、低延遲
- 缺點：需要 WebSocket 伺服器、較複雜
- 成本：中等
...

### 方案 B: Server-Sent Events (SSE)
...

### 方案 C: Long Polling
...

## 推薦方案：WebSocket + Redis Pub/Sub

### 架構設計
[詳細設計]

### 實作步驟
1. [步驟1]
2. [步驟2]
...
```

---

### 2. `/deepdive` - 深度探索程式碼庫

**用途**：深度探索程式碼庫和系統架構

**Token 預算**：依需求（觸發至少 5 次工具調用）

**適用場景**：
- ✅ 理解陌生的程式碼庫
- ✅ 分析系統架構
- ✅ 識別開發模式
- ✅ 發現潛在問題（重複程式碼、效能瓶頸、安全漏洞）

**基本用法**：
```bash
> /deepdive 交易系統的資料流和狀態管理

> /deepdive 尋找現有的認證實作模式

> /deepdive 分析首頁載入慢的原因
```

**指令會要求 Claude**：
1. 使用多種策略搜尋程式碼（關鍵字、檔案路徑、依賴分析）
2. 繪製系統架構圖（資料流、模組依賴、API 結構、DB schema）
3. 識別現有的開發模式
4. 分析潛在問題

**輸出範例**：
```markdown
# 交易系統深度分析

## 程式碼位置
- API 路由：src/app/api/trade/
- 前端元件：src/components/trade/
- 服務層：src/lib/services/trade-service.ts

## 資料流
1. 使用者 → TradeForm 元件
2. TradeForm → POST /api/trade
3. API → TradeService.create()
4. TradeService → Supabase + Redis
...

## 識別的模式
- 錯誤處理：使用 withAuthAndError 中間件 ✅
- 日誌：使用 apiLogger ✅
- 驗證：使用 Zod schema ✅

## 潛在問題
- ⚠️ 交易狀態更新沒有樂觀鎖定
- ⚠️ 快取失效策略不完整
```

---

### 3. `/safereview` - 安全程式碼審查

**用途**：在唯讀模式中進行程式碼審查

**Token 預算**：依需求

**適用場景**：
- ✅ Pull Request 審查
- ✅ 程式碼品質檢查
- ✅ 安全性審查
- ✅ 效能分析

**基本用法**：
```bash
# 建議在 Plan Mode 中使用
Shift+Tab

# 審查整個目錄
> /safereview src/app/api/trade/**/*.ts

# 審查特定檔案
> /safereview src/components/trade/TradeForm.tsx

# 審查最近的變更
> /safereview 審查我剛才新增的 API 路由
```

**指令會檢查**：
1. **程式碼品質** - coding style、TypeScript 類型、重複程式碼、複雜度
2. **架構一致性** - 專案模式、中間件、logger、CLAUDE.md 規範
3. **安全性** - SQL injection、XSS、敏感資料、認證授權
4. **效能** - N+1 查詢、重新渲染、快取、Bundle 大小
5. **測試** - 測試需求、邊界情況

**輸出範例**：
```markdown
# 程式碼審查：src/app/api/trade/route.ts

## ✅ 優點
- 正確使用 withAuthAndError 中間件
- TypeScript 類型定義完整
- 錯誤處理清晰

## ⚠️ 建議改進
1. **效能**：考慮增加快取層
   - 交易查詢可以快取 1 分鐘
   - 使用 Redis 減少資料庫負載

2. **安全性**：增加速率限制
   - 建議：每分鐘最多 60 次請求

## 🔴 必須修正
1. **並發控制**：缺少樂觀鎖定
   - 風險：同時更新可能導致資料不一致
   - 建議：使用版本號或時間戳

## 📝 範例程式碼
[改進的程式碼範例]
```

**重要**：此指令在 Plan Mode 中運行，**不會修改任何檔案**，只提供分析和建議。

---

### 4. `/archdesign` - 系統架構設計

**用途**：使用 ultrathink 進行系統架構設計

**Token 預算**：31,999（最大）

**適用場景**：
- ✅ 新系統的架構設計
- ✅ 現有系統的重構規劃
- ✅ 技術堆疊選擇
- ✅ 可擴展性設計

**基本用法**：
```bash
# 建議在 Plan Mode 中使用
Shift+Tab

> /archdesign 設計一個支援 100 萬使用者的社群平台

> /archdesign 重構現有的單體應用為微服務架構

> /archdesign 設計資料庫 sharding 策略
```

**指令會要求 Claude**：
1. **需求分析** - 功能、非功能需求、規模、限制
2. **架構選項評估** - 至少 3 種方案，包含優缺點、成本
3. **推薦方案詳細設計**：
   - 系統架構圖
   - 技術堆疊
   - 資料模型
   - API 設計
   - 安全性
   - 效能優化
4. **實作路線圖** - 分階段實作計劃
5. **風險評估** - 識別風險和應對策略

**輸出範例**：
```markdown
# 社群平台架構設計

## 需求分析
- 預期使用者：100 萬
- 日活躍用戶：10 萬
- 核心功能：貼文、留言、點讚、追蹤
...

## 架構方案評估

### 方案 A: 單體應用 + 垂直擴展
...

### 方案 B: 微服務 + 水平擴展
...

### 方案 C: Serverless 架構
...

## 推薦方案：微服務 + 水平擴展

### 系統架構圖
```
[ASCII 架構圖]
```

### 技術堆疊
- 前端：Next.js 15
- API Gateway：Nginx
- 服務：Node.js (用戶服務、貼文服務、通知服務)
- 資料庫：PostgreSQL (主資料) + Redis (快取)
- 訊息佇列：RabbitMQ
- 部署：Docker + Kubernetes
...

### 實作路線圖

#### 階段 1：MVP（3 個月）
- [ ] 使用者認證系統
- [ ] 貼文 CRUD
- [ ] 基礎互動（點讚、留言）
...
```

---

## 📖 使用方法

### 基本語法

```bash
# 無參數指令
> /指令名稱

# 帶參數指令
> /指令名稱 參數內容

# 多行參數（使用 \ 換行）
> /指令名稱 這是一個很長的參數 \
  可以分成多行 \
  更容易閱讀
```

### 查看可用指令

```bash
# 方法 1：輸入 / 然後等待自動完成
> /

# 方法 2：查看目錄
> !ls .claude/commands/

# 方法 3：使用 help
> /help
```

### 組合使用

```bash
# 組合 1：deepdive + ultraplan
> /deepdive 認證系統
# [等待結果]
> /ultraplan 基於剛才的分析，重構認證系統

# 組合 2：safereview + 修正
Shift+Tab  # Plan Mode
> /safereview src/app/api/auth/**/*.ts
# [等待審查結果]
Shift+Tab  # 退出 Plan Mode
> 根據審查建議修正問題

# 組合 3：archdesign + deepdive
> /archdesign 設計通知系統
# [等待設計完成]
> /deepdive 分析現有的通知實作
# [比較新舊設計]
```

---

## 🛠️ 建立自訂指令

### 1. 建立檔案

在 `.claude/commands/` 目錄中建立 Markdown 檔案。

```bash
# 檔案名稱決定指令名稱
# 例如：mycommand.md → /mycommand

touch .claude/commands/mycommand.md
```

### 2. 基本結構

```markdown
---
description: "指令的簡短描述（會顯示在自動完成中）"
---

# 指令標題（可選）

指令的詳細內容...

使用 $ARGUMENTS 接收參數。

## 範例格式

您可以使用任何 Markdown 格式來組織指令內容。
```

### 3. 參數處理

```markdown
---
description: "部署應用程式到指定環境"
---

# 部署指令

請將應用程式部署到 **$ARGUMENTS** 環境。

## 部署步驟：
1. 建置專案
2. 執行測試
3. 部署到 $ARGUMENTS
4. 驗證部署

## 注意事項：
- 確認 $ARGUMENTS 環境的設定正確
- 檢查環境變數
```

**使用範例**：
```bash
> /deploy production
# $ARGUMENTS 會被替換為 "production"
```

### 4. 進階參數

使用 `$1`, `$2`, `$3` 等接收個別參數。

```markdown
---
description: "建立新的 API 路由"
---

# 建立 API 路由

請建立以下 API 路由：
- HTTP 方法：$1
- 路徑：$2
- 功能：$3

## 要求：
- 使用 withAuthAndError 中間件
- 包含適當的錯誤處理
- 遵循專案規範
```

**使用範例**：
```bash
> /create-api GET /api/users 取得使用者清單
# $1 = "GET"
# $2 = "/api/users"
# $3 = "取得使用者清單"
```

---

## 🚀 進階功能

### Frontmatter 選項

```markdown
---
description: "指令描述"
# 未來可能支援的選項：
# extendedThinking: true
# model: "opus"
# maxTurns: 10
---

指令內容...
```

**注意**：目前 frontmatter 主要支援 `description`，其他選項可能在未來版本中支援。

### 嵌入魔法關鍵字

在指令內容中使用魔法關鍵字：

```markdown
---
description: "深度效能分析"
---

# 效能分析

ultrathink 請對以下進行深度效能分析：

$ARGUMENTS

請至少進行 5 次工具調用，deep dive 分析：
1. 資料庫查詢效能
2. 前端渲染效能
3. 網路請求優化
4. 快取策略
```

### 結構化輸出

使用 Markdown 結構引導 Claude 的輸出：

```markdown
---
description: "建立功能實作計劃"
---

# 功能實作計劃：$ARGUMENTS

## 1. 需求分析
[分析需求...]

## 2. 技術方案
### 選項 A
- 優點：
- 缺點：

### 選項 B
- 優點：
- 缺點：

## 3. 推薦方案
[詳細說明...]

## 4. 實作步驟
- [ ] 步驟 1
- [ ] 步驟 2
...

## 5. 測試計劃
...

## 6. 風險評估
| 風險 | 影響 | 機率 | 應對策略 |
|------|------|------|---------|
```

---

## 💡 最佳實踐

### 1. 命名規範

```bash
# ✅ 好的命名（清晰、簡短）
/review
/deploy
/analyze
/archdesign

# ❌ 避免的命名
/do-code-review-and-suggest-improvements  # 太長
/r  # 太短、不清楚
/123  # 無意義
```

### 2. 描述撰寫

```markdown
# ✅ 好的描述
description: "深度探索程式碼庫和系統架構"

# ❌ 不好的描述
description: "探索"  # 太簡短
description: "這個指令會深度探索程式碼庫，分析系統架構，識別模式，找出問題，並提供詳細的報告，包含程式碼引用和改進建議..."  # 太長
```

### 3. 參數設計

```markdown
# ✅ 清楚的參數說明
請審查以下程式碼：**$ARGUMENTS**

# ✅ 提供預設值
目標環境：$1（預設：staging）

# ✅ 驗證參數
如果 $ARGUMENTS 為空，請審查最近的變更。
```

### 4. 整合專案規範

```markdown
---
description: "建立新的 React 元件"
---

# 建立元件：$ARGUMENTS

請遵循專案規範：
- ✅ 使用 TypeScript
- ✅ 禁止使用漸層和 emoji
- ✅ 使用 SVG 圖示
- ✅ 遵循 CLAUDE.md 中的規範

[其他內容...]
```

### 5. 包含範例

在指令中提供範例幫助 Claude 理解：

```markdown
---
description: "建立 API 路由"
---

# 建立 API 路由

請建立：$ARGUMENTS

## 參考範例

類似於現有的 `/api/trade/route.ts`：
- 使用 `withAuthAndError` 中間件
- Zod schema 驗證
- `apiLogger` 記錄
- 統一錯誤處理

[更多內容...]
```

---

## 🔍 疑難排解

### 指令不顯示在自動完成中

**原因**：
- 檔案不在 `.claude/commands/` 目錄
- 檔案副檔名不是 `.md`
- 語法錯誤

**解決方法**：
```bash
# 1. 檢查檔案位置
ls .claude/commands/

# 2. 檢查檔案內容
cat .claude/commands/your-command.md

# 3. 重新啟動 Claude Code
# 退出並重新啟動
```

---

### 參數沒有被替換

**原因**：
- 拼寫錯誤（`$ARGUMENT` vs `$ARGUMENTS`）
- 使用了錯誤的參數變數（`$4` 但只傳了 2 個參數）

**解決方法**：
```bash
# 檢查指令檔案中的變數名稱
grep '\$' .claude/commands/your-command.md

# 確認使用正確的變數
# 所有參數：$ARGUMENTS
# 個別參數：$1, $2, $3...
```

---

### 指令執行但結果不符預期

**原因**：
- 指令內容不夠明確
- 缺少上下文或範例
- 沒有整合專案規範

**解決方法**：
1. **增加具體性**：
```markdown
# ❌ 不夠具體
請審查程式碼

# ✅ 具體
請審查以下程式碼，檢查：
1. TypeScript 類型
2. 錯誤處理
3. 安全性
4. 效能
```

2. **提供範例**：
```markdown
參考現有的實作：src/app/api/trade/route.ts
```

3. **整合專案規範**：
```markdown
請遵循 CLAUDE.md 中的開發規範。
```

---

### 如何測試新指令

```bash
# 1. 建立測試指令
echo '---
description: "測試指令"
---

# 測試

這是一個測試指令，參數是：$ARGUMENTS
' > .claude/commands/test.md

# 2. 使用指令
> /test 測試參數

# 3. 檢查結果
# Claude 應該回應包含「測試參數」的內容

# 4. 滿意後刪除測試指令
rm .claude/commands/test.md
```

---

## 📊 指令設計決策樹

```
需要重複使用這個工作流程嗎？
│
├─ 否 → 直接在對話中使用
│
└─ 是 → 建立自訂指令
    │
    ├─ 需要複雜的邏輯？
    │  ├─ 是 → 使用魔法關鍵字（ultrathink, deep dive）
    │  └─ 否 → 簡單的指令內容
    │
    ├─ 需要參數？
    │  ├─ 是 → 使用 $ARGUMENTS 或 $1, $2, $3
    │  └─ 否 → 固定內容
    │
    └─ 需要結構化輸出？
       ├─ 是 → 使用 Markdown 模板
       └─ 否 → 自由格式
```

---

## 🎓 進階範例

### 範例 1：多階段工作流程

```markdown
---
description: "完整的功能開發流程"
---

# 功能開發：$ARGUMENTS

## 階段 1：探索現有程式碼
deep dive 尋找與 「$ARGUMENTS」 相關的現有實作模式。

## 階段 2：規劃設計
ultrathink 設計實作方案，考慮：
- 可維護性
- 效能影響
- 測試策略
- 專案規範（CLAUDE.md）

## 階段 3：提供實作計劃
請提供詳細的實作步驟：
- [ ] 步驟 1
- [ ] 步驟 2
...

## 階段 4：風險評估
列出潛在風險和應對策略。
```

### 範例 2：條件式指令

```markdown
---
description: "智慧程式碼審查"
---

# 程式碼審查

請審查：$ARGUMENTS

## 審查級別

如果是 API 路由，特別檢查：
- 中間件使用
- 錯誤處理
- 認證授權

如果是 React 元件，特別檢查：
- Props 類型定義
- 效能優化（useMemo, useCallback）
- 無障礙性（a11y）

如果是資料庫相關，特別檢查：
- SQL injection 風險
- N+1 查詢問題
- 索引優化
```

### 範例 3：團隊協作指令

```markdown
---
description: "準備程式碼給團隊審查"
---

# PR 準備：$ARGUMENTS

請幫我準備以下變更給團隊審查：

## 1. 變更摘要
請總結這次變更的內容。

## 2. 測試證明
列出已測試的場景：
- [ ] 正常情況
- [ ] 邊界情況
- [ ] 錯誤處理

## 3. 影響分析
- API 變更：
- 資料庫變更：
- 效能影響：
- 破壞性變更：

## 4. PR 描述草稿
請產生 PR 描述，包含：
- 問題描述
- 解決方案
- 測試計劃
- 截圖（如果是 UI 變更）

## 5. 檢查清單
- [ ] 遵循 CLAUDE.md 規範
- [ ] 測試通過
- [ ] 類型檢查通過
- [ ] 文檔更新
```

---

## 🔗 相關資源

- [魔法關鍵字完整參考](./magic-keywords-reference.md)
- [工作流程總覽](./README.md)
- [Claude Code 官方文檔](https://docs.claude.com/en/docs/claude-code/slash-commands)
- [專案開發規範](../../CLAUDE.md)

---

## 📝 貢獻指南

### 新增團隊共用指令

1. 在 `.claude/commands/` 建立新指令
2. 測試指令是否正常運作
3. 更新此文檔
4. 提交到 git

```bash
# 範例流程
# 1. 建立指令
vim .claude/commands/new-command.md

# 2. 測試
> /new-command 測試參數

# 3. 提交
git add .claude/commands/new-command.md
git commit -m "新增 /new-command 指令"
```

### 改進現有指令

如果發現指令可以改進：
1. 直接編輯指令檔案
2. 測試改進效果
3. 提交變更

```bash
# 編輯指令
vim .claude/commands/ultraplan.md

# 測試
> /ultraplan 測試改進後的效果

# 提交
git add .claude/commands/ultraplan.md
git commit -m "改進 /ultraplan 指令的輸出格式"
```

---

**提示**：自訂指令是提升開發效率的強大工具。隨著專案發展，持續新增和改進指令，打造適合您團隊的工作流程！
