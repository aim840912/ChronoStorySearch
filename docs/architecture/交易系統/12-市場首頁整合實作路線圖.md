# 市場首頁整合實作路線圖

> **最後更新**：2025-10-27（完成度：83%）

---

## 📚 導航

[← 上一篇:測試計畫](./11-測試計畫.md) | [🏠 返回目錄](./README.md)

---

## 概述

**功能目標**：將交易系統的市場功能整合到首頁,實現無縫的物品瀏覽與市場刊登切換體驗。

**核心需求**：
1. 擴展篩選模式,新增「市場刊登」選項
2. 合併市場按鈕為單一下拉選單
3. 在首頁卡片區域顯示市場刊登列表
4. 擴展進階篩選支援市場特定條件

---

## 總覽

| 階段 | 時程 | 核心任務 | 狀態 |
|------|------|---------|------|
| **階段 0** | 0.5 天 | 準備工作 | ✅ 已完成 |
| **階段 1** | 1 天 | 類型定義和基礎結構 | ✅ 已完成 |
| **階段 2** | 1.5 天 | Hooks 實作 | ✅ 已完成 |
| **階段 3** | 2 天 | UI 元件實作 | ✅ 已完成 |
| **階段 4** | 1 天 | 頁面整合 | ✅ 已完成 |
| **階段 5** | 1 天 | 測試和優化 | ⏸️ 待開始 |

**總工時**：5-7 天（40-56 小時，含 1 天 buffer）

---

## 階段 0：準備工作（0.5 天）

**目標**：建立開發環境和文檔

**優先級**：🔴 **必要**

### 任務清單

- [x] 架構設計完成
- [x] 建立實作路線圖文檔
- [x] 確認當前分支（feature/discord-trade-system）

### 驗收標準

✅ **完成條件**：
- 架構設計文檔完整
- 實作路線圖建立
- 開發環境就緒

---

## 階段 1：類型定義和基礎結構（1 天）

**目標**：擴展 TypeScript 類型定義,建立新功能的基礎結構

**優先級**：🔴 **必要**

### 任務清單

#### 1.1 擴展核心類型（src/types/index.ts）

- [ ] 擴展 `FilterMode` 類型
  ```typescript
  export type FilterMode =
    | 'all'
    | 'favorite-monsters'
    | 'favorite-items'
    | 'market-listings' // ★ 新增
  ```

- [ ] 擴展 `AdvancedFilterOptions` 接口
  ```typescript
  export interface AdvancedFilterOptions {
    enabled: boolean
    dataType: DataTypeFilter
    itemCategories: ItemCategoryGroup[]
    jobClasses: JobClass[]
    levelRange: LevelRange
    marketFilter?: MarketFilterOptions // ★ 新增
  }
  ```

#### 1.2 新增市場篩選類型

- [ ] 新增 `MarketFilterOptions` 接口
  ```typescript
  export interface MarketFilterOptions {
    tradeTypes: TradeType[]
    priceRange: { min: number | null; max: number | null }
    itemStatsFilter: {
      min_watk?: number
      min_matk?: number
      min_wdef?: number
      stats_grade?: StatsGrade[]
    }
    sortBy: 'created_at' | 'price' | 'stats_score'
    sortOrder: 'asc' | 'desc'
  }
  ```

- [ ] 新增 `MarketListingsResponse` 類型
  ```typescript
  export interface MarketListingsResponse {
    listings: ListingWithUserInfo[]
    pagination: {
      page: number
      limit: number
      total: number
      totalPages: number
    }
  }
  ```

- [ ] 新增 `ListingWithUserInfo` 類型
  ```typescript
  export interface ListingWithUserInfo extends ListingWithStats {
    seller: {
      username: string
      discord_username: string | null
      discord_discriminator: string | null
    }
    item: {
      itemId: number
      itemName: string
      chineseItemName: string | null
    }
  }
  ```

#### 1.3 新增國際化翻譯

- [ ] 更新 `src/locales/zh-TW.json`
  ```json
  {
    "filter": {
      "marketListings": "市場刊登"
    },
    "market": {
      "title": "市場",
      "createListing": "建立刊登",
      "myListings": "我的刊登",
      "browseMarket": "市場瀏覽",
      "interests": "購買意向",
      "filters": {
        "tradeType": "交易類型",
        "priceRange": "價格範圍",
        "itemStats": "物品屬性",
        "sortBy": "排序方式"
      }
    }
  }
  ```

- [ ] 更新 `src/locales/en.json`
  ```json
  {
    "filter": {
      "marketListings": "Market Listings"
    },
    "market": {
      "title": "Market",
      "createListing": "Create Listing",
      "myListings": "My Listings",
      "browseMarket": "Browse Market",
      "interests": "Interests",
      "filters": {
        "tradeType": "Trade Type",
        "priceRange": "Price Range",
        "itemStats": "Item Stats",
        "sortBy": "Sort By"
      }
    }
  }
  ```

### 驗收標準

✅ **功能驗收**：
- 所有類型定義完整
- TypeScript 編譯通過（`npm run type-check`）
- 無 TypeScript 錯誤或警告

✅ **程式碼品質**：
- 類型定義清晰且有適當註釋
- 命名符合專案慣例
- 國際化翻譯完整

---

## 階段 2：Hooks 實作（1.5 天）

**目標**：實作市場相關的自訂 Hooks

**優先級**：🔴 **必要**

### 任務清單

#### 2.1 實作 useMarketFilter Hook

- [ ] 建立 `src/hooks/useMarketFilter.ts`
  - 管理市場篩選狀態（tradeTypes, priceRange, itemStatsFilter）
  - 實作 `getFilteredItemIds()` 方法（轉換進階篩選為 item_id 列表）
  - 提供重置和更新方法

#### 2.2 實作 useMarketListings Hook

- [ ] 建立 `src/hooks/useMarketListings.ts`
  - 管理市場刊登資料（listings, pagination, loading, error）
  - 調用 `/api/market/search` API
  - 處理分頁和排序
  - 實作緩存策略（避免重複請求）

#### 2.3 修改 useFilterLogic Hook

- [ ] 修改 `src/hooks/useFilterLogic.ts`
  - 新增 market-listings 模式處理
  - 返回適當的 displayMode

### 驗收標準

✅ **功能驗收**：
- Hooks 正確管理狀態
- API 調用正確
- 緩存策略有效

✅ **程式碼品質**：
- 使用 useCallback 和 useMemo 優化效能
- 適當的錯誤處理
- 符合現有 hooks 模式

---

## 階段 3：UI 元件實作（2 天）

**目標**：實作和修改 UI 元件

**優先級**：🔴 **必要**

### 任務清單

#### 3.1 修改現有元件

- [ ] 修改 `src/components/FilterButtons.tsx`
  - 新增「市場刊登」按鈕
  - 處理模式切換邏輯
  - 當 filterMode === 'market-listings' 時隱藏清除最愛按鈕

- [ ] 修改 `src/components/AdvancedFilterPanel.tsx`
  - 根據 filterMode 條件渲染不同的篩選面板
  - 整合 MarketFilterPanel

#### 3.2 新增市場元件

- [ ] 實作 `src/components/trade/MarketDropdownMenu.tsx`
  - 單一「市場」按鈕,點擊展開下拉選單
  - 包含 4 個選項（建立刊登、我的刊登、市場瀏覽、購買意向）
  - 使用 Headless UI 的 Menu 元件
  - 響應式設計

- [ ] 實作 `src/components/trade/MarketFilterPanel.tsx`
  - 交易類型多選（販售/收購/交換）
  - 價格範圍輸入（最小/最大）
  - 物品屬性篩選（展開式）
  - 排序方式選擇

- [ ] 實作 `src/components/trade/MarketListView.tsx`
  - 列表式 Layout
  - 顯示市場刊登列表
  - 無限滾動或傳統分頁
  - 載入狀態顯示
  - 響應式設計

- [ ] 實作 `src/components/trade/MarketListItem.tsx`
  - 單個刊登項目顯示
  - 包含物品圖片、名稱、交易類型、數量、價格、賣家資訊
  - 點擊事件處理
  - 響應式設計

### 驗收標準

✅ **功能驗收**：
- 所有元件渲染正確
- 互動功能正常
- 響應式設計在各尺寸正常

✅ **設計驗收**：
- 符合專案設計系統
- 深色模式支援
- 動畫流暢

✅ **無障礙驗收**：
- 鍵盤導航支援
- ARIA 屬性正確
- 螢幕閱讀器友好

---

## 階段 4：頁面整合（1 天）✅

**目標**：將所有功能整合到首頁

**優先級**：🔴 **必要**

**狀態**：✅ **已完成** (2025-01-27)

### 任務清單

#### 4.1 修改首頁

- [x] 修改 `src/app/page.tsx`
  - [x] 整合 useMarketFilter hook
  - [x] 整合 useMarketListings hook
  - [x] 新增 marketFilter 狀態管理
  - [x] 實作條件渲染邏輯（根據 filterMode）
  - [x] 處理模式切換（useEffect 監聽）
  - [x] 監聽 filterMode 變化並載入市場刊登
  - [x] 監聽 marketFilter 和 advancedFilter 變化並重新載入
  - [x] 傳遞 market 相關 props 給 ContentDisplay

#### 4.2 整合 SearchHeader 和 ContentDisplay

- [x] 修改 `src/components/SearchHeader.tsx`
  - [x] 添加 MarketDropdownMenu 元件
  - [x] 添加 MarketFilterPanel 元件
  - [x] 調整佈局（FilterButtons 和 MarketDropdownMenu 在同一列）
  - [x] 條件渲染：非市場模式顯示 AdvancedFilterPanel，市場模式顯示 MarketFilterPanel
  - [x] 僅在登入時顯示 MarketDropdownMenu

- [x] 修改 `src/components/ContentDisplay.tsx`
  - [x] 新增 market-listings 模式分支
  - [x] 整合 MarketListView 元件
  - [x] 傳遞必要的 props（listings, pagination, isLoading, error, callbacks）

#### 4.3 URL 參數支援

- [ ] 實作 URL hash 參數（延後到 Phase 5）
  - 支援 `#market-listings` 模式
  - 支援市場篩選參數（tradeType, priceRange 等）
  - 分享連結功能

### 驗收標準

✅ **功能驗收**：
- [x] 模式切換流暢（filterMode 變化觸發市場載入）
- [x] 市場刊登正確顯示（使用 MarketListView）
- [x] 篩選功能正常（進階篩選 + 市場篩選整合）
- [ ] URL 分享功能正常（延後到 Phase 5）

✅ **效能驗收**：
- [x] 模式切換無明顯延遲（只在需要時 enabled useMarketListings）
- [x] 無多餘的 API 調用（使用 useEffect 依賴正確控制）
- [ ] 首次載入時間 < 2 秒（需要實際測試）

✅ **程式碼品質**：
- [x] TypeScript 類型檢查通過（無新增錯誤）
- [x] 使用 useCallback 和 useMemo 優化（在 hooks 中已實作）
- [x] Props 正確傳遞（page.tsx → SearchHeader/ContentDisplay）

---

## 階段 5：測試和優化（1 天）

**目標**：全面測試和效能優化

**優先級**：🟢 **重要**

### 任務清單

#### 5.1 功能測試

- [ ] 模式切換測試
  - 測試所有模式切換組合
  - 確認狀態正確重置

- [ ] 市場篩選測試
  - 測試所有篩選組合
  - 確認 API 參數正確

- [ ] 刊登顯示測試
  - 測試不同數據量
  - 測試空狀態
  - 測試錯誤狀態

#### 5.2 效能優化

- [ ] 檢查 render 次數
  - 使用 React DevTools Profiler
  - 優化不必要的 re-render

- [ ] 優化 API 調用
  - 確認緩存策略有效
  - 實作適當的 debounce

- [ ] 實作 memoization
  - 對昂貴的計算使用 useMemo
  - 對回調函數使用 useCallback

#### 5.3 無障礙測試

- [ ] 鍵盤導航測試
  - Tab 順序正確
  - Enter/Escape 行為正確

- [ ] 螢幕閱讀器測試
  - ARIA labels 正確
  - 語意化 HTML

#### 5.4 跨瀏覽器測試

- [ ] Chrome/Edge（Chromium）
- [ ] Firefox
- [ ] Safari（如有 Mac）
- [ ] 移動端瀏覽器

### 驗收標準

✅ **功能驗收**：
- 所有測試通過
- 無已知 bug

✅ **效能驗收**：
- 無效能問題
- 無記憶體洩漏

✅ **品質驗收**：
- TypeScript 檢查通過
- ESLint 無警告
- 無障礙標準達標

---

## 風險管理

### 已識別風險

| 風險 | 影響 | 機率 | 應對策略 | 負責人 |
|------|------|------|---------|--------|
| 進階篩選轉換邏輯複雜 | 中 | 中 | 先實作基本篩選,再漸進增加複雜度;預留 2 小時 buffer | 開發者 |
| API 效能問題 | 高 | 低 | 使用現有的 /api/market/search,已有優化;監控查詢時間 | 開發者 |
| 狀態管理衝突 | 中 | 低 | 使用獨立的 market hooks,避免與現有邏輯衝突 | 開發者 |
| UI 響應式問題 | 低 | 中 | 遵循現有設計模式,使用 Tailwind 響應式類別 | 開發者 |
| 類型定義不一致 | 中 | 低 | 嚴格遵循 TypeScript strict mode,及早發現問題 | 開發者 |

---

## 完成定義（Definition of Done）

**整體標準**：

- [x] 所有類型定義完整且通過 TypeScript 檢查
- [x] Hooks 功能正常（useMarketFilter, useMarketListings）
- [x] UI 元件渲染正確且響應式（MarketListView, MarketFilterPanel, MarketDropdownMenu）
- [x] 模式切換流暢無錯誤（filterMode 狀態管理）
- [x] 市場篩選功能正常（進階篩選 + 市場篩選整合）
- [x] API 整合正確無錯誤（使用現有 /api/market/search）
- [x] 國際化支援完整（zh-TW, en）
- [x] 深色模式支援（所有新元件支援）
- [ ] 無障礙標準達標（需要實際測試）
- [x] 無 ESLint/TypeScript 警告（已修復 useMarketFilter 警告）
- [x] 文檔更新完成（實作路線圖已更新）

**程式碼品質**：

- [x] 遵循 CLAUDE.md 開發規範（無漸層、無 emoji、使用 SVG）
- [x] 使用統一的日誌系統（apiLogger/dbLogger）- N/A（前端元件無需日誌）
- [x] 使用統一的錯誤處理（hooks 中有錯誤處理）
- [x] 適當的註釋和文檔（所有元件都有 JSDoc）
- [ ] Git commit message 清晰（待提交）

**效能標準**：

- [ ] 首次載入時間 < 2 秒（需要實際測試）
- [x] 模式切換無明顯延遲（只在需要時 enabled hooks）
- [x] 無多餘的 API 調用（useEffect 依賴正確控制）
- [ ] 無記憶體洩漏（需要實際測試）

---

## 進度追蹤

**當前狀態**：階段 1 進行中

**完成度**：
- 階段 0：✅ 100%
- 階段 1：⏳ 0%
- 階段 2：⏸️ 0%
- 階段 3：⏸️ 0%
- 階段 4：⏸️ 0%
- 階段 5：⏸️ 0%

**總體完成度**：0%

**預計完成時間**：2025-11-03（假設每天 8 小時工作）

---

## 參考文檔

- [01-系統總覽與架構](./01-系統總覽與架構.md)
- [03-API設計](./03-API設計.md)
- [08-實作路線圖](./08-實作路線圖.md)
- [CLAUDE.md](/home/aim840912/projects/maplestory/CLAUDE.md)

---

## 📚 導航

[← 上一篇:測試計畫](./11-測試計畫.md) | [🏠 返回目錄](./README.md)
