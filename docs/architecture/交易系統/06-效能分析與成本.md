# 效能分析與成本預估

> **最後更新**：2025-10-26

---

## 📚 導航

[← 上一篇:安全與可靠性](./05-安全與可靠性.md) | [🏠 返回目錄](./README.md) | [下一篇:Bot防護與監控 →](./07-Bot防護與監控.md)

---

## 實際流量分析

### 監測數據概覽

**監測期間**：2025-10-20 至 2025-10-25（5 天）

```
總訪問數：7,794 visitors
平均訪問數：1,559 visitors/天
數據來源：Vercel Analytics
```

### 流量組成分析

#### 訪客類型推估

| 類型 | 估計比例 | 數量/天 | 計算依據 |
|------|---------|---------|---------|
| **真實用戶** | 20-30% | 312-468 | 正常互動行為（登入、瀏覽、登記意向） |
| **SEO 爬蟲** | 10-15% | 156-234 | GoogleBot, BingBot（User-Agent 可識別） |
| **惡意爬蟲** | 30-40% | 468-624 | curl, wget, 自動化工具（User-Agent 可識別） |
| **無效請求** | 20-30% | 312-468 | 重複訪問、無 User-Agent、API 掃描 |

**關鍵發現**：
- 🔴 **60-80% 流量是 Bot**（1,169 bots/天，僅 390 真實用戶/天）
- 🔴 **MAU 推算**：390 DAU / 0.3 日活率 = **1,300 MAU**
- 🔴 **實際流量遠超原估算**（原估算 800 MAU，實際 1,300 MAU = 163%）

#### 端點流量分布

| 端點類別 | 估計請求數/天 | Bot 比例 | Bot 請求/天 | 真實請求/天 |
|---------|-------------|---------|------------|------------|
| **trending（公開）** | 1,200 | 80-90% | 960-1,080 | 120-240 |
| **市場列表（認證）** | 3,744 | 30-40% | 1,123-1,498 | 2,246-2,621 |
| **建立刊登（認證）** | 62 | 10-20% | 6-12 | 50-56 |
| **登記意向（認證）** | 374 | 10-20% | 37-75 | 299-337 |
| **查看聯絡方式（認證）** | 187 | 5-10% | 9-19 | 168-178 |
| **OAuth 登入** | 156 | 40-50% | 62-78 | 78-94 |
| **其他 API** | 611 | 50-60% | 306-367 | 244-305 |
| **總計** | **6,334** | **~60%** | **~3,800** | **~2,500** |

**關鍵發現**：
1. **trending 端點是 Bot 流量重災區**：80-90% 請求來自 Bot
2. **認證端點 Bot 比例較低**：但仍有 10-40% Bot（帳號註冊機器人）
3. **OAuth 登入端點也有 40-50% Bot**：說明 Discord 登入無法完全阻止 Bot

### Redis 命令數消耗分析

#### 當前狀態（無 Bot 過濾）

```typescript
// Rate Limiting 命令數（固定窗口算法：INCR + EXPIRE）
const rateLimitOps = {
  trending: 1200 * 2,           // = 2,400 命令（80-90% 是 Bot）
  marketList: 3744 * 2,         // = 7,488 命令
  createListing: 62 * 2,        // = 124 命令
  registerInterest: 374 * 2,    // = 748 命令
  viewContact: 187 * 2,         // = 374 命令
  oauthLogin: 156 * 2,          // = 312 命令
  otherAPIs: 611 * 2,           // = 1,222 命令
}

const total = Object.values(rateLimitOps).reduce((a, b) => a + b, 0)
// = 12,668 命令/天

// ⚠️ 實際監測為 10,668 命令/天（可能部分端點未啟用 Rate Limit）
```

**結論**：當前 Redis 使用量 10,668 命令/天 = **107% 使用率**（已超標）

#### Bot 過濾後預估（階段性）

| 階段 | 過濾策略 | Bot 減少率 | Redis 命令/天 | 使用率 | 狀態 |
|------|---------|-----------|--------------|-------|------|
| **當前** | 無過濾 | 0% | 10,668 | 107% | ❌ 已超標 |
| **Discord 單獨** | OAuth 認證門檻 | 3-5% | 10,350 | 103% | ❌ 仍超標 |
| **階段 1** | User-Agent 基礎過濾 | 40-50% | 7,000-8,000 | 70-80% | ⚠️ 接近上限 |
| **階段 2** | User-Agent 全面檢測 | 60-70% | 5,000-6,000 | 50-60% | ✅ 可運行 |
| **階段 3** | User-Agent + 行為檢測 | 70-80% | 4,000-5,000 | 40-50% | ✅ 穩定 |
| **階段 4** | Cloudflare Bot Protection | 85-95% | 2,000-3,000 | 20-30% | ✅ 理想 |

**關鍵結論**：
- 🔴 **Discord 登入單獨無法解決 Redis 超標問題**（僅減少 3-5%）
- ✅ **必須搭配 Bot Detection**（至少實作階段 2，減少 60-70% Bot）
- ✅ **建議方案**：Discord 登入 + User-Agent 全面檢測 → 5,000-6,000 命令/天

### 流量異常模式識別

**觀察到的異常行為**（基於端點分析）：

1. **高頻訪問**：
   - 現象：單一 IP 在 1 小時內請求 > 50 次
   - 比例：估計 20-30% 的 Bot 流量
   - 特徵：User-Agent 為 curl, wget, python-requests

2. **掃描行為**：
   - 現象：快速訪問多個不同端點（1 分鐘內 > 20 個端點）
   - 比例：估計 10-15% 的 Bot 流量
   - 特徵：User-Agent 為 headless, selenium, phantom

3. **無 User-Agent 請求**：
   - 現象：完全沒有 User-Agent header
   - 比例：估計 5-10% 的流量
   - 判定：100% 為 Bot 或惡意請求

4. **SEO 爬蟲**（合法）：
   - 來源：GoogleBot, BingBot, BaiduSpider
   - 比例：10-15% 的流量
   - 處理：白名單允許（有利於 SEO）

5. **OAuth Bot**（新發現）：
   - 現象：自動化 Discord 帳號通過 OAuth 登入
   - 比例：OAuth 登入端點 40-50% 是 Bot
   - 特徵：短時間內大量新帳號註冊
   - **說明：Discord 登入無法完全阻止 Bot**

### MAU 修正與容量規劃

#### 原估算 vs 實際數據

| 指標 | 原估算 | 實際數據 | 差異 | 原因分析 |
|------|-------|---------|------|---------|
| Daily Visitors | ~320 | **1,559** | +387% | **高 Bot 流量未預期** |
| 真實 DAU | 240 | 312-468 | +30-95% | Bot 過濾後接近原估算 |
| MAU | 800 | **1,200-1,600** | +50-100% | 實際用戶略高於預期 |
| Redis 命令 | 6,800 | **10,668** | +57% | Bot 流量消耗大量配額 |

#### 修正後的容量規劃

**保守估計（Bot 過濾 70%）**：
```
真實 MAU：1,200-1,400
真實 DAU：360-420（30% 日活率）
API 請求：2,500-3,000/天（過濾 Bot 後）
Redis 命令：5,000-6,000/天（50-60% 使用率）
狀態：✅ 免費版可穩定運行
```

**樂觀估計（Bot 過濾 80%）**：
```
真實 MAU：1,000-1,200
真實 DAU：300-360
API 請求：2,000-2,500/天
Redis 命令：4,000-5,000/天（40-50% 使用率）
狀態：✅ 免費版餘裕充足
```

### 應對建議（優先級排序）

#### 🔴 立即執行（P0 - 0-1 週）

1. **實作基礎 Bot Detection（User-Agent 過濾）**
   - 黑名單：curl, wget, python-requests, headless
   - 白名單：GoogleBot, BingBot（SEO 爬蟲）
   - 無 User-Agent 直接拒絕
   - 預期效果：減少 40-50% Bot → Redis 降至 7,000-8,000 命令/天

2. **降級閾值調整**
   - 從 90% → 70%（提早觸發降級）
   - 確保 Redis 不會超標

#### 🟡 短期執行（P1 - 2-4 週）

3. **Discord OAuth 實作**
   - 完整 OAuth 2.0 流程
   - Session 管理與 Token 刷新
   - 預期效果：減少 3-5% Bot → 配合 P0 可達 50-60% 使用率

4. **User-Agent 全面檢測**
   - 所有端點應用 Bot Detection
   - 行為模式分析（高頻訪問、掃描行為）
   - 預期效果：減少 60-70% Bot → Redis 降至 5,000-6,000 命令/天

#### 🟢 中期執行（P2 - 1-2 個月）

5. **IP 信譽評分系統**
   - 記錄 IP 歷史行為
   - 計算 IP 信譽分數（0-100）
   - 低信譽 IP 自動限流
   - 預期效果：減少 70-80% Bot → Redis 降至 4,000-5,000 命令/天

6. **流量監控儀表板**
   - 實時 Bot 流量比例
   - Redis 使用率趨勢
   - 自動化告警

#### ⚪ 長期優化（P3 - 可選）

7. **Cloudflare Bot Protection**
   - 終極解決方案（免費版有限制）
   - 預期效果：減少 85-95% Bot → Redis 降至 2,000-3,000 命令/天

### 核心結論

**問題診斷**：
> **60-80% 流量是 Bot**，導致免費版 Redis 超標 6.68%

**解決路徑**：
```
當前狀態（1,559 visitors/天，107% Redis）
  ↓
階段 1：基礎 Bot Detection（1 週內）
  → User-Agent 過濾
  → Redis 降至 7,000-8,000 命令/天（70-80% 使用率）
  → ⚠️ 臨時緩解，仍需後續優化
  ↓
階段 2：Discord 登入 + 全面檢測（2-4 週）
  → OAuth 認證 + User-Agent 全面檢測
  → Redis 降至 5,000-6,000 命令/天（50-60% 使用率）
  → ✅ 可穩定運行在免費版
  ↓
階段 3：IP 信譽評分（1-2 個月）
  → 行為檢測 + IP 信譽
  → Redis 降至 4,000-5,000 命令/天（40-50% 使用率）
  → ✅ 免費版長期穩定
```

**重要提醒**：
- 🔴 **Discord 登入不是銀彈**：僅減少 3-5% Bot，無法單獨解決 Redis 超標問題
- ✅ **Bot Detection 是必要條件**：至少需要實作階段 2（User-Agent 全面檢測）
- ✅ **免費版可行**：Discord + Bot Detection 組合可降至 50-60% Redis 使用率

---

## 成本預估

### 免費額度評估

#### Supabase 免費版
- 資料庫空間：500 MB（當前使用 < 50 MB）✅
- 月活躍用戶：50,000（當前 1,200-1,600）✅

#### Vercel 免費版
- 流量：100 GB/月（當前 69 GB/月）⚠️
- 函數執行時間：100 小時/月 ✅

#### Upstash Redis 免費版
- 命令數：10,000/天
- **當前使用**：10,668/天（107%）❌
- **儲存空間**：256 MB（當前使用 < 1 MB）✅

**關鍵評估**：

```typescript
// Discord 登入效果（修正評估）
當前：10,668 命令/天（107% 超標）
Discord 單獨：10,350 命令/天（103% 仍超標）❌
  → 僅減少 3-5% Bot
  → 無法單獨解決 Redis 超標問題

// Bot Detection 效果
Bot Detection (階段 2)：5,000-6,000 命令/天（50-60%）✅
  → 減少 60-70% Bot
  → 可穩定運行在免費版

// 組合方案（推薦）
Discord + Bot Detection：5,000-6,000 命令/天（50-60%）✅
  → Discord 登入提供身份驗證
  → Bot Detection 解決 Redis 超標
  → 兩者缺一不可
```

**重要結論**：
- 🔴 **Discord 登入不是銀彈**：僅減少 3-5% Bot，無法單獨解決 Redis 超標問題
- 🔴 **Bot Detection 是必要條件**：必須實作 User-Agent 全面檢測（階段 2）
- ✅ **免費版可行**：Discord + Bot Detection 組合可降至 50-60% Redis 使用率
- ⚠️ **實作順序**：建議先實作 Bot Detection（1 週），再實作 Discord 登入（2-3 週）

### 升級決策流程圖

#### 監控指標與警報閾值

**核心監控指標**：

| 服務 | 指標 | 免費額度 | 警報閾值 | 危險閾值 | 行動 |
|------|------|---------|---------|---------|------|
| **Upstash Redis** | 命令數/天 | 10,000 | 8,000 (80%) | 9,500 (95%) | 實作 Bot Detection |
| **Vercel** | 流量/月 | 100 GB | 80 GB (80%) | 95 GB (95%) | 優化圖片/啟用 CDN |
| **Supabase** | MAU | 50,000 | 40,000 (80%) | 47,500 (95%) | 考慮升級 |
| **Supabase** | 儲存空間 | 500 MB | 400 MB (80%) | 475 MB (95%) | 清理舊資料 |
| **Vercel** | 函數執行時間 | 100 小時 | 80 小時 (80%) | 95 小時 (95%) | 優化查詢 |

**監控實作範例**：

```typescript
// src/lib/monitoring/quota-monitor.ts
import { redis } from '@/lib/redis'

interface QuotaStatus {
  service: string
  metric: string
  current: number
  limit: number
  usage_percentage: number
  status: 'safe' | 'warning' | 'danger' | 'exceeded'
  alert_required: boolean
}

export async function checkRedisQuota(): Promise<QuotaStatus> {
  const key = 'quota:redis:daily_commands'
  const count = await redis.get<number>(key) || 0
  const limit = 10000
  const percentage = (count / limit) * 100

  let status: QuotaStatus['status']
  if (percentage >= 100) status = 'exceeded'
  else if (percentage >= 95) status = 'danger'
  else if (percentage >= 80) status = 'warning'
  else status = 'safe'

  return {
    service: 'Upstash Redis',
    metric: 'Daily Commands',
    current: count,
    limit,
    usage_percentage: percentage,
    status,
    alert_required: status !== 'safe'
  }
}

export async function checkVercelBandwidth(): Promise<QuotaStatus> {
  // 需要從 Vercel API 或日誌中獲取
  // 這裡使用估算值作為範例
  const currentGB = 69  // 從實際監控數據獲取
  const limitGB = 100
  const percentage = (currentGB / limitGB) * 100

  let status: QuotaStatus['status']
  if (percentage >= 100) status = 'exceeded'
  else if (percentage >= 95) status = 'danger'
  else if (percentage >= 80) status = 'warning'
  else status = 'safe'

  return {
    service: 'Vercel',
    metric: 'Bandwidth',
    current: currentGB,
    limit: limitGB,
    usage_percentage: percentage,
    status,
    alert_required: status !== 'safe'
  }
}

export async function checkAllQuotas(): Promise<QuotaStatus[]> {
  const results = await Promise.all([
    checkRedisQuota(),
    checkVercelBandwidth(),
    // 可擴展：checkSupabaseMAU(), checkSupabaseStorage()
  ])

  return results
}

// 每日定時檢查（使用 Vercel Cron Jobs）
export async function dailyQuotaCheck(): Promise<void> {
  const quotas = await checkAllQuotas()
  const alerts = quotas.filter(q => q.alert_required)

  if (alerts.length > 0) {
    await sendQuotaAlerts(alerts)
  }
}

async function sendQuotaAlerts(alerts: QuotaStatus[]): Promise<void> {
  // 發送到 Discord Webhook 或電子郵件
  const message = alerts.map(alert => {
    const emoji = alert.status === 'exceeded' ? '🔴' : alert.status === 'danger' ? '⚠️' : '🟡'
    return `${emoji} ${alert.service} - ${alert.metric}: ${alert.usage_percentage.toFixed(1)}% (${alert.current}/${alert.limit})`
  }).join('\n')

  console.log('[Quota Alert]', message)
  // 實作實際的警報發送邏輯
}
```

#### 升級決策樹

```typescript
// 自動升級建議系統
interface UpgradeRecommendation {
  service: string
  should_upgrade: boolean
  reason: string
  estimated_cost: number  // USD/月
  priority: 'low' | 'medium' | 'high' | 'critical'
}

export async function generateUpgradeRecommendations(): Promise<UpgradeRecommendation[]> {
  const quotas = await checkAllQuotas()
  const recommendations: UpgradeRecommendation[] = []

  // Redis 決策
  const redis = quotas.find(q => q.service === 'Upstash Redis')
  if (redis) {
    if (redis.status === 'exceeded') {
      recommendations.push({
        service: 'Upstash Redis',
        should_upgrade: true,
        reason: '命令數已超標，系統可能受限。建議先實作 Bot Detection（免費），若仍超標則升級。',
        estimated_cost: 10,  // Pro 200K 方案
        priority: 'critical'
      })
    } else if (redis.status === 'danger' && redis.usage_percentage >= 95) {
      recommendations.push({
        service: 'Upstash Redis',
        should_upgrade: false,
        reason: '接近上限，優先實作 Bot Detection 降低 40-50% 使用量。',
        estimated_cost: 0,
        priority: 'high'
      })
    }
  }

  // Vercel 決策
  const vercel = quotas.find(q => q.service === 'Vercel')
  if (vercel && vercel.status === 'exceeded') {
    recommendations.push({
      service: 'Vercel',
      should_upgrade: true,
      reason: '流量已超標。檢查項目：1) 啟用圖片優化 2) 減少 API 請求 3) 考慮升級 Pro。',
      estimated_cost: 20,  // Pro 方案
      priority: 'high'
    })
  }

  return recommendations
}
```

#### 成本比較表

**Upstash Redis 方案對比**：

| 方案 | 命令數/天 | 儲存空間 | 月費 | 適用場景 |
|------|----------|---------|------|---------|
| **Free** | 10,000 | 256 MB | $0 | 當前流量 + Bot Detection |
| **Pay as you go** | 100,000+ | 1 GB+ | $0.2/10K 命令 | 流量波動大 |
| **Pro 200K** | 200,000 | 2 GB | $10 | 穩定中等流量 |
| **Pro 3M** | 3,000,000 | 10 GB | $60 | 高流量 |

**實際成本估算**：

```typescript
// 基於當前流量 10,668 命令/天（實作 Bot Detection 後降至 5,000-6,000）
免費版：$0
  → 實作 Bot Detection 後：5,000-6,000 命令/天（50-60% 使用率）
  → 可穩定運行 ✅

// 若不實作 Bot Detection，直接升級
Pay as you go：$0.2/10K 命令 × 10,668 命令/天 × 30 天 = $6.4/月
Pro 200K：$10/月（固定成本，更划算）

// 若流量成長至 20,000 命令/天
Pay as you go：$0.2/10K × 20,000 × 30 = $12/月
Pro 200K：$10/月（更划算）✅
```

**Vercel 方案對比**：

| 方案 | 流量 | 函數執行時間 | 月費 | 適用場景 |
|------|------|------------|------|---------|
| **Hobby** | 100 GB | 100 小時 | $0 | 當前流量（69 GB/月）|
| **Pro** | 1 TB | 1000 小時 | $20 | 高流量或商業用途 |
| **Enterprise** | 自訂 | 自訂 | 聯繫報價 | 企業級 |

#### Vercel Functions 執行時間分析

**交易系統 UI 架構對成本的影響**：

交易系統的 UI 實作方式（Modal vs 獨立頁面）會直接影響 Vercel Serverless Functions 的執行時間。

**方案 A：Modal 模式（推薦）**

```typescript
// 用戶完整流程的 Function 調用

// 1. 訪問主頁
GET / → SSR 渲染主頁 → 100ms

// 2. 打開市場 Modal（客戶端切換，無 Function 調用）
// 僅觸發 API 請求
GET /api/market → 查詢 Supabase → 200ms

// 3. 查看刊登詳情（Modal 內嵌，無額外 SSR）
GET /api/listings/[id] → 100ms

// 4. 登記購買意向
POST /api/interests → 150ms

總計：550ms ≈ 0.0092 分鐘/用戶
```

**方案 B：獨立頁面模式**

```typescript
// 用戶完整流程的 Function 調用

// 1. 訪問主頁
GET / → SSR 渲染主頁 → 100ms

// 2. 導航到市場頁面（觸發 SSR）
GET /market → SSR 渲染市場頁面 → 150ms
GET /api/market → 初始資料載入 → 200ms

// 3. 導航到刊登詳情頁（觸發 SSR）
GET /listings/[id] → SSR 渲染詳情頁 → 100ms
GET /api/listings/[id] → 載入完整資料 → 100ms

// 4. 登記購買意向
POST /api/interests → 150ms

總計：800ms ≈ 0.0133 分鐘/用戶
差異：+250ms (+45%)
```

**實際使用場景估算**：

假設平均用戶行為（真實交易用戶 7,000 人/月）：
- 瀏覽市場 5 次（查看不同物品）
- 查看刊登詳情 3 次
- 登記意向 1 次

```typescript
// Modal 模式
主頁 SSR：100ms × 1 = 100ms
市場 API：200ms × 5 = 1,000ms
刊登詳情 API：100ms × 3 = 300ms
登記意向 API：150ms × 1 = 150ms
總計：1,550ms = 0.026 分鐘/用戶

月度總計：7,000 × 0.026 = 182 分鐘 ≈ 3.0 小時/月
佔 Vercel 額度：3.0 / 100 = 3%

// 獨立頁面模式
主頁 SSR：100ms × 1 = 100ms
市場 SSR：150ms × 5 = 750ms（每次導航都觸發）
市場 API：200ms × 5 = 1,000ms
刊登詳情 SSR：100ms × 3 = 300ms（每次導航都觸發）
刊登詳情 API：100ms × 3 = 300ms
登記意向 API：150ms × 1 = 150ms
總計：2,600ms = 0.043 分鐘/用戶

月度總計：7,000 × 0.043 = 301 分鐘 ≈ 5.0 小時/月
佔 Vercel 額度：5.0 / 100 = 5%

差異：+2.0 小時/月 (+67%)
```

**成本對比表**：

| 項目 | Modal 模式 | 獨立頁面 | 差異 |
|------|-----------|---------|------|
| **Bandwidth（頻寬）** | 5.74 GB/月 | 6.02 GB/月 | +0.28 GB (+5%) |
| **Function Time（函數時間）** | 3.0 小時/月 | 5.0 小時/月 | **+2.0 小時 (+67%)** |
| **總 Vercel 額度消耗** | 8% | 11% | +3% |

**結論與建議**：

✅ **推薦採用 Modal 模式**：
- 節省約 2 小時 Vercel Functions 執行時間/月（約 2% 額度）
- 當前 Vercel 使用安全（預估總使用率 < 10%）
- 符合專案現有 UI 架構（`BaseModal`、`ModalManager`）
- 開發速度更快（重用現有元件）

⚠️ **流量增長考量**：
```
若用戶數增長 3 倍（21,000 用戶）：

Modal 模式：
└─ Functions：3.0 × 3 = 9.0 小時/月（9% 使用率）✅

獨立頁面模式：
└─ Functions：5.0 × 3 = 15.0 小時/月（15% 使用率）⚠️

差異擴大至 6 小時/月
```

**Supabase 方案對比**：

| 方案 | MAU | 儲存空間 | 資料庫 | 月費 | 適用場景 |
|------|-----|---------|-------|------|---------|
| **Free** | 50,000 | 500 MB | 500 MB | $0 | 當前流量（1,200-1,600 MAU）|
| **Pro** | 100,000 | 8 GB | 8 GB | $25 | 成長期 |
| **Team** | 無限 | 100 GB | 100 GB | $599 | 企業級 |

#### 升級時機建議

**立即行動（0-2 週）**：
1. ✅ **實作 Bot Detection 階段 1**（User-Agent 過濾）
   - 成本：$0（開發 1 週）
   - 效果：減少 40-50% Redis 使用量
   - 優先級：🔴 Critical

2. ⚠️ **啟用 Vercel 圖片優化**
   - 成本：$0（內建功能）
   - 效果：減少 10-20% 流量
   - 優先級：🟡 Medium

**短期計劃（2-4 週）**：
3. ✅ **實作 Bot Detection 階段 2**（行為模式檢測）
   - 成本：$0（開發 2-4 週）
   - 效果：減少 60-70% Redis 使用量
   - 優先級：🔴 Critical

4. ✅ **實作 Discord OAuth**
   - 成本：$0（開發 2-3 週）
   - 效果：提升安全性，減少 3-5% Bot
   - 優先級：🟢 High

**中期規劃（1-3 個月）**：
5. ⚠️ **持續監控與優化**
   - 監控 Redis、Vercel、Supabase 使用率
   - 若 Bot Detection 後仍超標，考慮升級 Redis Pro $10/月
   - 若 Vercel 流量超過 90 GB/月，考慮升級 Pro $20/月

**升級觸發條件**：

```typescript
interface UpgradeTrigger {
  service: string
  condition: string
  action: string
  estimated_cost: number
}

const UPGRADE_TRIGGERS: UpgradeTrigger[] = [
  {
    service: 'Upstash Redis',
    condition: 'Bot Detection 實作後仍超過 9,000 命令/天（90%）持續 7 天',
    action: '升級至 Pro 200K 方案',
    estimated_cost: 10
  },
  {
    service: 'Vercel',
    condition: '流量連續 3 個月超過 95 GB/月（95%）',
    action: '升級至 Pro 方案',
    estimated_cost: 20
  },
  {
    service: 'Supabase',
    condition: 'MAU 超過 40,000（80%）或儲存空間超過 400 MB（80%）',
    action: '升級至 Pro 方案',
    estimated_cost: 25
  }
]

// 自動檢查升級觸發條件
export async function checkUpgradeTriggers(): Promise<UpgradeTrigger[]> {
  const triggered: UpgradeTrigger[] = []
  const quotas = await checkAllQuotas()

  // Redis 檢查
  const redis = quotas.find(q => q.service === 'Upstash Redis')
  if (redis && redis.usage_percentage >= 90) {
    // 檢查是否已實作 Bot Detection
    const botDetectionEnabled = await isBotDetectionEnabled()
    if (botDetectionEnabled) {
      const trigger = UPGRADE_TRIGGERS.find(t => t.service === 'Upstash Redis')
      if (trigger) triggered.push(trigger)
    }
  }

  // Vercel 檢查
  const vercel = quotas.find(q => q.service === 'Vercel')
  if (vercel && vercel.usage_percentage >= 95) {
    const trigger = UPGRADE_TRIGGERS.find(t => t.service === 'Vercel')
    if (trigger) triggered.push(trigger)
  }

  return triggered
}

async function isBotDetectionEnabled(): Promise<boolean> {
  // 檢查 middleware 是否已啟用 Bot Detection
  // 實作邏輯...
  return false
}
```

### 成本最佳化策略

**零成本優化**（優先執行）：
1. ✅ **Bot Detection**：減少 60-70% 無效流量
2. ✅ **圖片優化**：使用 Next.js Image 組件（自動 WebP 轉換）
3. ✅ **API 快取**：合理設置 Cache-Control headers
4. ✅ **資料庫查詢優化**：減少不必要的查詢、使用索引

**低成本優化**（$0-$20/月）：
1. ⚠️ **Cloudflare CDN**：免費版提供無限流量（需遷移 DNS）
2. ⚠️ **Upstash Redis Pro**：$10/月支援 200K 命令/天
3. ⚠️ **R2 圖片儲存**：$0（10 GB 免費）+ $0.015/GB（超出部分）

**預估總成本**（實作所有優化後）：

| 場景 | 流量 | Redis | Vercel | Supabase | 總成本 |
|------|------|-------|--------|---------|-------|
| **當前（未優化）** | 1,559/天 | 超標 ❌ | $0 | $0 | $0（不可持續）|
| **Bot Detection 後** | 1,559/天 | $0 ✅ | $0 | $0 | **$0** ✅ |
| **流量 2x 成長** | 3,000/天 | $10 | $0 | $0 | **$10** |
| **流量 5x 成長** | 7,500/天 | $10 | $20 | $0 | **$30** |
| **流量 10x 成長** | 15,000/天 | $60 | $20 | $25 | **$105** |

**結論**：
- 🎯 **最佳策略**：實作 Bot Detection（免費）→ 可穩定運行在全免費版
- 📈 **成長空間**：免費版可支撐至 3,000 MAU（2x 成長）
- 💰 **升級成本**：若需升級，月費 $10-30（中等流量）或 $105（高流量）

---


---

## 📚 導航

[← 上一篇:安全與可靠性](./05-安全與可靠性.md) | [🏠 返回目錄](./README.md) | [下一篇:Bot防護與監控 →](./07-Bot防護與監控.md)
